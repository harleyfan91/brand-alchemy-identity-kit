/**
 * Browser-safe entry: re-exports only the dev gallery PDF document (no Node `registerCoreKitPdfFonts` / `renderToBuffer` chain).
 */
export { CtaFrameDevGalleryDocument } from './CtaFrameDevGalleryDocument.js'
